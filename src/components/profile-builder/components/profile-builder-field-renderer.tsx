/**
 * Re-export shim — komponen ini telah dipecah ke subfolder field-renderer/.
 * File ini dipertahankan agar import yang sudah ada (misal dari verification page)
 * tidak perlu diubah.
 */
export { default } from "./field-renderer";
export { dropdownColorMap } from "./field-renderer/shared";
